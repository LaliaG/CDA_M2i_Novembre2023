// Programme principal

// Saisies
let chaineAdn = saisieChaineAdn("Saisir la chaine : ")
let sequence = saisieChaineAdn("Saisir la séquence : ")

console.log("chaine :", chaineAdn)
console.log("sequence :", sequence)

// Calcul
let pourcentage = pourcentageSequence(sequence, chaineAdn)
let (occurrences * sequence.length / chaine.length * 100)
let (chaine.match(regex)?.length ??)
let (chaine.match(new RegExp(sequence, "g"))?.length ??)* sequence.length / chaine.length * 100) = pourcentageSequence(sequence, chaineAdn)
// Affichage du résultat
console.log(`Il y a ${pourcentage}% de \"${sequence}\" dans la chaine \"${chaineAdn}\"`)
    
    
//let chaine = "actgactggg"
//let sequence = "act"

let regex = new RegExp(sequence, "g") // expression regex que vous verrez plus tard
let occurrences = chaine.match(regex)?.length ?? 0 // compte le nombre de fois où la chaine apparaît

console.log(occurrences)



console.log(pourcentage)



